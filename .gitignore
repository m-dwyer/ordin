node_modules/
.DS_Store
*.log

# User-specific config overrides
projects.local.yaml
.env
.env.local

# Build artefacts (none expected; Bun runs TS directly)
dist/
*.tsbuildinfo

# Test coverage output
coverage/

# Scratch output from dev-loop fixtures (see `bun run fixture:setup`)
.scratch/

# LiteLLM disk cache — regenerated on first eval run
.cache/

# Initial conversation prompt — kept locally, not committed.
init_prompt.txt
