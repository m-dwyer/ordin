# Intentionally minimal. The @opentui/solid Babel transform preload
# moved into `src/cli/index.ts` as an explicit import so the CLI entry
# is self-contained — same dev semantics (the import runs before any
# `.tsx` is loaded), and the compiled binary no longer crashes on a
# preload reference its bundle can't resolve.
